/**
 * Site contact — update `CONTACT_EMAIL` when needed.
 * Resume PDF lives in `public/`; update `RESUME_PATH` if the filename changes.
 */
export const CONTACT_EMAIL = "Chandini.krishnegowda99@gmail.com";

/** Public URL for resume PDF (`public/chandini_krishnegowda_mentorque (3).pdf`) */
export const RESUME_PATH = `/${encodeURIComponent("chandini_krishnegowda_mentorque (3).pdf")}`;
